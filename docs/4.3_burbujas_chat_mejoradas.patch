diff --git a/frontend/call.html b/frontend/call.html
index e6c0a6d..036d5f9 100644
--- a/frontend/call.html
+++ b/frontend/call.html
@@ -100,22 +100,79 @@
       .hidden { display: none; }
 
       .conversation {
-        display: grid;
-        gap: 12px;
+        display: flex;
+        flex-direction: column;
+        gap: 14px;
         max-height: 510px;
         overflow: auto;
         padding-right: 6px;
       }
+      .bubble-row {
+        display: flex;
+        gap: 10px;
+        align-items: flex-end;
+        max-width: 100%;
+      }
+      .bubble-row.user { flex-direction: row-reverse; }
+      .bubble-row.system { justify-content: center; }
+      .avatar {
+        flex: 0 0 auto;
+        width: 30px;
+        height: 30px;
+        border-radius: 50%;
+        display: flex;
+        align-items: center;
+        justify-content: center;
+        font-size: 0.72rem;
+        font-weight: 700;
+        color: var(--bg);
+      }
+      .avatar.user { background: var(--accent-2); }
+      .avatar.assistant { background: var(--accent); }
+      .bubble-col { display: flex; flex-direction: column; max-width: 78%; }
+      .bubble-row.user .bubble-col { align-items: flex-end; }
+      .bubble-row.assistant .bubble-col { align-items: flex-start; }
+      .bubble-sender {
+        font-size: 0.72rem;
+        font-weight: 700;
+        color: var(--muted);
+        margin: 0 4px 4px;
+        text-transform: uppercase;
+        letter-spacing: 0.03em;
+      }
       .bubble {
-        padding: 14px 16px;
+        padding: 12px 16px;
         border-radius: 18px;
         border: 1px solid var(--line);
-        max-width: 86%;
         line-height: 1.5;
       }
-      .user { background: rgba(110,168,255,0.14); margin-left: auto; }
-      .assistant { background: rgba(78,214,167,0.12); }
-      .system { background: rgba(255,255,255,0.05); max-width: 100%; color: var(--muted); }
+      .user .bubble { background: rgba(110,168,255,0.16); border-bottom-right-radius: 6px; }
+      .assistant .bubble { background: rgba(78,214,167,0.12); border-bottom-left-radius: 6px; }
+      .system .bubble { background: rgba(255,255,255,0.05); color: var(--muted); max-width: 100%; font-size: 0.85rem; }
+      .bubble-time {
+        font-size: 0.68rem;
+        color: var(--muted);
+        margin: 4px 4px 0;
+      }
+      .typing-dots {
+        display: inline-flex;
+        gap: 4px;
+        padding: 4px 2px;
+      }
+      .typing-dots span {
+        width: 6px;
+        height: 6px;
+        border-radius: 50%;
+        background: var(--accent);
+        opacity: 0.4;
+        animation: typing-pulse 1.1s infinite ease-in-out;
+      }
+      .typing-dots span:nth-child(2) { animation-delay: 0.15s; }
+      .typing-dots span:nth-child(3) { animation-delay: 0.3s; }
+      @keyframes typing-pulse {
+        0%, 80%, 100% { opacity: 0.25; transform: translateY(0); }
+        40% { opacity: 1; transform: translateY(-3px); }
+      }
 
       .toolbar { display: flex; flex-wrap: wrap; gap: 10px; align-items: center; }
       .small { font-size: 0.9rem; color: var(--muted); }
@@ -276,14 +333,71 @@
       const VAD_MIN_RECORDING_MS = 700;
       const VAD_MAX_RECORDING_MS = 20000;
 
+      const senderLabel = (role) => (role === 'user' ? 'Paciente' : role === 'assistant' ? 'Agente' : 'Sistema');
+      const senderInitials = (role) => (role === 'user' ? 'P' : 'A');
+      const formatTime = () => new Date().toLocaleTimeString('es-CO', { hour: '2-digit', minute: '2-digit' });
+
       const addBubble = (role, text, extra = '') => {
+        const row = document.createElement('div');
+        row.className = `bubble-row ${role}`;
+
+        if (role !== 'system') {
+          const avatar = document.createElement('div');
+          avatar.className = `avatar ${role}`;
+          avatar.textContent = senderInitials(role);
+          row.appendChild(avatar);
+        }
+
+        const col = document.createElement('div');
+        col.className = 'bubble-col';
+
+        if (role !== 'system') {
+          const sender = document.createElement('div');
+          sender.className = 'bubble-sender';
+          sender.textContent = senderLabel(role);
+          col.appendChild(sender);
+        }
+
         const bubble = document.createElement('div');
         bubble.className = `bubble ${role}`;
-        bubble.innerHTML = `<strong>${role === 'user' ? 'Paciente' : role === 'assistant' ? 'Agente' : 'Sistema'}:</strong> ${text}${extra ? `<div class="small" style="margin-top: 8px;">${extra}</div>` : ''}`;
-        conversation.appendChild(bubble);
+        bubble.innerHTML = `${text}${extra ? `<div class="small" style="margin-top: 8px;">${extra}</div>` : ''}`;
+        col.appendChild(bubble);
+
+        if (role !== 'system') {
+          const time = document.createElement('div');
+          time.className = 'bubble-time';
+          time.textContent = formatTime();
+          col.appendChild(time);
+        }
+
+        row.appendChild(col);
+        conversation.appendChild(row);
         conversation.scrollTop = conversation.scrollHeight;
       };
 
+      let typingRow = null;
+      const showTyping = () => {
+        if (typingRow) return;
+        typingRow = document.createElement('div');
+        typingRow.className = 'bubble-row assistant';
+        typingRow.innerHTML = `
+          <div class="avatar assistant">A</div>
+          <div class="bubble-col">
+            <div class="bubble-sender">Agente</div>
+            <div class="bubble assistant">
+              <span class="typing-dots"><span></span><span></span><span></span></span>
+            </div>
+          </div>`;
+        conversation.appendChild(typingRow);
+        conversation.scrollTop = conversation.scrollHeight;
+      };
+      const hideTyping = () => {
+        if (typingRow) {
+          typingRow.remove();
+          typingRow = null;
+        }
+      };
+
       const chooseSpanishVoice = () => {
         if (!synth) return;
         const voices = synth.getVoices();
@@ -473,6 +587,7 @@
         callStatus.textContent = 'Procesando turno... (el agente está pensando la respuesta)';
         sendBtn.disabled = true;
         sendBtn.textContent = 'Enviando…';
+        showTyping();
 
         try {
           const response = await fetch('/call/session/turn', {
@@ -487,6 +602,7 @@
           }
 
           const payload = await response.json();
+          hideTyping();
           addBubble('assistant', payload.assistant_text, payload.decision ? `Decisión: ${payload.decision.label}` : '');
           renderDecision(payload.decision);
           renderReferences(payload.references);
@@ -498,6 +614,7 @@
         } catch (error) {
           callStatus.textContent = `Error de red al procesar el turno: ${error.message}`;
         } finally {
+          hideTyping();
           sendBtn.disabled = false;
           sendBtn.textContent = 'Enviar turno';
         }
